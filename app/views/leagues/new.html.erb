<h1>Create a league</h1>
<p><%= current_user.email %> is about to create a league up in err.</p>

<%= simple_form_for @league do |f| %>
	<%= f.input :name %>
	<%= f.button :submit %>
	<%= f.hidden_field :manager_id, value: current_user.id %>
<% end %>